apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-template-operator.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-template-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-template-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
